<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="DialogRadioButton" parent="android:Widget.Material.CompoundButton.RadioButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">@dimen/dialog_padding_horizontal</item>
        <item name="android:paddingEnd">@dimen/dialog_padding_horizontal</item>
        <item name="android:textSize">@dimen/dialog_radio_button_label_text_size</item>
    </style>

    <style name="AppTheme.Dialog">

    </style>

    <style name="AppTheme.Dialog.Layout">
        <item name="android:layout_marginStart">@dimen/dialog_layout_horizontal_margin</item>
        <item name="android:layout_marginEnd">@dimen/dialog_layout_horizontal_margin</item>
        <item name="android:layout_marginTop">@dimen/dialog_layout_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/dialog_layout_vertical_margin</item>
    </style>

    <!-- This will set the fade in animation on all your activities by default -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
